# LEARNING_GAMMA

Simple web app wrapper around `yt-dlp` for downloading videos.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Run

```bash
python app.py
```

Then open http://localhost:5000 and paste a URL.
